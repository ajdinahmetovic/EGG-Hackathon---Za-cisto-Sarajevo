


const initialState = {
    roomId: null,
    loadingMessage: null,
    dropLocationXY: null,
    bombs: null,

};


export default function (state = initialState, action) {
    switch (action.type) {
    
        

        default:
            return state
    }
}